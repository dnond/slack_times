require 'spec_helper'

describe SlackTimes do
  it 'has a version number' do
    expect(SlackTimes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
